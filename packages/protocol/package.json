{
  "name": "@mirri-ai/protocol",
  "version": "0.3.2",
  "private": true,
  "description": "Shared REST + WS protocol schemas (envelope, error codes, pagination, ws-control) for the mirri-code daemon.",
  "license": "MIT",
  "author": "Mirri AI",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mirri-ai/mirricode.git",
    "directory": "packages/protocol"
  },
  "bugs": {
    "url": "https://github.com/mirri-ai/mirricode/issues"
  },
  "type": "module",
  "imports": {
    "#/*": [
      "./src/*.ts",
      "./src/*/index.ts"
    ]
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ulid": "^3.0.1",
    "zod": "^4.3.6"
  }
}
