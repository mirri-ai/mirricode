# Config Section Manifest
#
# Generated by scripts/gen-config-manifest.mts — do not edit by hand.
# Regenerate with: pnpm --filter @mirri-ai/agent-core-v2 gen:config-manifest
#
# One [table] per registered config section, in the on-disk config.toml shape
# (snake_case keys). Un-commented assignments are registered defaults;
# commented "# field: type" lines describe the remaining schema fields.
# Values resolve as: default -> config.toml -> env overlay -> memory.

# Index (21 sections · 2 overlay(s))
#   background               src/agent/task/configSection.ts
#   cron                     src/app/cron/configSection.ts
#   defaultPermissionMode    src/agent/permissionMode/configSection.ts
#   defaultPlanMode          src/agent/plan/configSection.ts
#   experimental             src/app/flag/flag.ts
#   extraAgentDirs           src/workspace/workspaceAgentProfileLoader/configSection.ts
#   extraSkillDirs           src/app/skillCatalog/configSection.ts
#   hooks                    src/agent/externalHooks/configSection.ts
#   image                    src/agent/media/configSection.ts
#   loopControl              src/agent/loop/configSection.ts
#   mcp                      src/app/mcpConfig/configSection.ts
#   mergeAllAvailableSkills  src/app/skillCatalog/configSection.ts
#   modelCatalog             src/app/kosongConfig/configSection.ts
#   models                   src/app/kosongConfig/configSection.ts
#   permission               src/agent/permissionRules/configSection.ts
#   providers                src/app/kosongConfig/configSection.ts
#   services                 src/app/auth/configSection.ts
#   subagent                 src/session/subagent/configSection.ts
#   task                     src/agent/task/configSection.ts
#   thinking                 src/app/kosongConfig/configSection.ts
#   tools                    src/agent/toolPolicy/configSection.ts
#   (overlay) servicesCredentialEnvOverlay  src/app/auth/configSection.ts
#   (overlay) mirriModelEnvOverlay  src/app/kosongConfig/envOverlay.ts

# ##########################################################################
# background
#   owner: src/agent/task/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     keep_alive_on_exit <- MIRRICODE_BACKGROUND_KEEP_ALIVE_ON_EXIT (custom parse)
#     max_running_tasks <- MIRRICODE_BACKGROUND_MAX_RUNNING_TASKS (custom parse)
# ##########################################################################

[background]
# max_running_tasks: integer
# keep_alive_on_exit: boolean
# bash_auto_background_on_timeout: boolean
# bash_task_timeout_s: integer
# kill_grace_period_ms: integer
# print_wait_ceiling_s: integer
# print_background_mode: "exit" | "drain" | "steer"
# print_max_turns: integer

# ##########################################################################
# cron
#   owner: src/app/cron/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     debug <- MIRRI_CRON_DEBUG (custom parse)
#     no_jitter <- MIRRI_CRON_NO_JITTER (custom parse)
#     no_stale <- MIRRI_CRON_NO_STALE (custom parse)
#     disabled <- MIRRI_DISABLE_CRON (custom parse)
#     manual_tick <- MIRRI_CRON_MANUAL_TICK (custom parse)
#     clock <- MIRRI_CRON_CLOCK
#     poll_interval_ms <- MIRRI_CRON_POLL_INTERVAL_MS (custom parse)
# ##########################################################################

[cron]
# (schema uses transforms; fields below come from the registered default)
debug = false
no_jitter = false
no_stale = false
disabled = false
manual_tick = false

# ##########################################################################
# defaultPermissionMode (config.toml: default_permission_mode)
#   owner: src/agent/permissionMode/configSection.ts
#   scope: core
# ##########################################################################

# default_permission_mode: "manual" | "auto" | "yolo"

# ##########################################################################
# defaultPlanMode (config.toml: default_plan_mode)
#   owner: src/agent/plan/configSection.ts
#   scope: core
# ##########################################################################

default_plan_mode = false

# ##########################################################################
# experimental
#   owner: src/app/flag/flag.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[experimental]
# <name>: boolean

# ##########################################################################
# extraAgentDirs (config.toml: extra_agent_dirs)
#   owner: src/workspace/workspaceAgentProfileLoader/configSection.ts
#   scope: core
# ##########################################################################

extra_agent_dirs = []

# ##########################################################################
# extraSkillDirs (config.toml: extra_skill_dirs)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
# ##########################################################################

extra_skill_dirs = []

# ##########################################################################
# hooks
#   owner: src/agent/externalHooks/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[hooks]
# (schema uses transforms; see the owner file)

# ##########################################################################
# image
#   owner: src/agent/media/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     max_edge_px <- MIRRI_IMAGE_MAX_EDGE_PX (custom parse)
#     read_byte_budget <- MIRRI_IMAGE_READ_BYTE_BUDGET (custom parse)
# ##########################################################################

[image]
# max_edge_px: integer
# read_byte_budget: integer

# ##########################################################################
# loopControl (config.toml: loop_control)
#   owner: src/agent/loop/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml · stripEnv
#   env:
#     max_steps_per_turn <- MIRRI_LOOP_MAX_STEPS_PER_TURN (custom parse)
#     max_retries_per_step <- MIRRI_LOOP_MAX_RETRIES_PER_STEP (custom parse)
# ##########################################################################

[loop_control]
# max_steps_per_turn: integer
# max_retries_per_step: integer
# max_ralph_iterations: integer
# reserved_context_size: integer
# compaction_trigger_ratio: number

# ##########################################################################
# mcp
#   owner: src/app/mcpConfig/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     startup_timeout_ms <- MIRRI_MCP_STARTUP_TIMEOUT_MS (custom parse)
#     tool_timeout_ms <- MIRRI_MCP_TOOL_TIMEOUT_MS (custom parse)
# ##########################################################################

[mcp]
# startup_timeout_ms: integer
# tool_timeout_ms: integer

# ##########################################################################
# mergeAllAvailableSkills (config.toml: merge_all_available_skills)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
# ##########################################################################

merge_all_available_skills = true

# ##########################################################################
# modelCatalog (config.toml: model_catalog)
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
# ##########################################################################

[model_catalog]
# refresh_interval_ms: integer
# refresh_on_start: boolean

# ##########################################################################
# models
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[models]

# one [models."<name>"] table per entry:
# [models."<name>"]
  # provider_id: string
  # base_url: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # protocol: "anthropic" | "openai" | "openai_responses" | "google-genai"
  # name: string
  # provider: string
  # model: string
  # max_context_size: integer
  # max_input_size: integer
  # max_output_size: integer
  # capabilities: string[]
  # display_name: string
  # description: string
  # reasoning_key: string
  # adaptive_thinking: boolean
  # beta_api: boolean
  # support_efforts: string[]
  # default_effort: string
  # off_effort: string
  # overrides: object
    # max_context_size: integer
    # max_input_size: integer
    # max_output_size: integer
    # capabilities: string[]
    # display_name: string
    # description: string
    # reasoning_key: string
    # adaptive_thinking: boolean
    # support_efforts: string[]
    # default_effort: string
    # off_effort: string

# ##########################################################################
# permission
#   owner: src/agent/permissionRules/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[permission]
# rules: object[] — one entry per item:
  # decision: "allow" | "deny" | "ask"
  # scope: "turn-override" | "session-runtime" | "project" | "user" (default: "user")
  # pattern: string
  # reason: string

# ##########################################################################
# providers
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml · stripEnv
#   env:
#     __mirri_env__.api_key <- MIRRICODE_MODEL_API_KEY
#     __mirri_env__.type <- MIRRICODE_MODEL_PROVIDER_TYPE
#     __mirri_env__.base_url <- MIRRICODE_MODEL_BASE_URL
# ##########################################################################

[providers]

# one [providers."<name>"] table per entry:
# [providers."<name>"]
  # model_source: "static" | "discover" | "oauth-catalog"
  # base_url: string
  # custom_headers: record<string, string>
  # default_model: string
  # type: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # env: record<string, string>
  # source: record<string, any>
  # deleted_models: string[]

# ##########################################################################
# services
#   owner: src/app/auth/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml · stripEnv
#   env:
#     moonshot_search.base_url <- MIRRI_WEB_SEARCH_BASE_URL (custom parse)
#     moonshot_search.api_key <- MIRRI_WEB_SEARCH_API_KEY (custom parse)
#     moonshot_fetch.base_url <- MIRRI_WEB_FETCH_BASE_URL (custom parse)
#     moonshot_fetch.api_key <- MIRRI_WEB_FETCH_API_KEY (custom parse)
# ##########################################################################

[services]

# [services.moonshot_search]
  # base_url: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # custom_headers: record<string, string>

# [services.moonshot_fetch]
  # base_url: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # custom_headers: record<string, string>

# ##########################################################################
# subagent
#   owner: src/session/subagent/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     timeout_ms <- MIRRI_SUBAGENT_TIMEOUT_MS (custom parse)
# ##########################################################################

[subagent]
timeout_ms = 7200000

# ##########################################################################
# task
#   owner: src/agent/task/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     keep_alive_on_exit <- MIRRICODE_BACKGROUND_KEEP_ALIVE_ON_EXIT (custom parse)
#     max_running_tasks <- MIRRICODE_BACKGROUND_MAX_RUNNING_TASKS (custom parse)
# ##########################################################################

[task]
# max_running_tasks: integer
# keep_alive_on_exit: boolean
# bash_auto_background_on_timeout: boolean
# bash_task_timeout_s: integer
# kill_grace_period_ms: integer
# print_wait_ceiling_s: integer
# print_background_mode: "exit" | "drain" | "steer"
# print_max_turns: integer

# ##########################################################################
# thinking
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     forced_effort <- MIRRICODE_MODEL_THINKING_EFFORT
# ##########################################################################

[thinking]
# enabled: boolean
# effort: string
# forced_effort: string
# keep: string

# ##########################################################################
# tools
#   owner: src/agent/toolPolicy/configSection.ts
#   scope: core
# ##########################################################################

[tools]
# enabled: string[]
# disabled: string[]
