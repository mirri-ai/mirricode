{
  "name": "@mirri-ai/migration-legacy",
  "version": "0.1.15",
  "description": "Migrate kimi-cli (~/.mirricode/) data into mirri-code (~/.mirricode-code/).",
  "license": "MIT",
  "type": "module",
  "private": true,
  "files": [
    "dist"
  ],
  "imports": {
    "#/*": [
      "./src/*.ts",
      "./src/*/index.ts"
    ]
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mirri-ai/agent-core": "workspace:^",
    "smol-toml": "^1.6.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@mirri-ai/kaos": "workspace:^"
  }
}
