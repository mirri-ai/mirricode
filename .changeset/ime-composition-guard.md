---
"@mirri-ai/mirri-code": patch
---

web: Fix IME composition Enter being treated as a submit or confirm key in web UI inputs, so Chinese input method candidates confirm instead of accidentally submitting answers and dialogs.