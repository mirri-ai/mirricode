---
"@mirri-ai/mirri-code": patch
---

web: Make the real model name editable in the model settings form, and split the Add Model dialog into separate alias and model name fields so a local alias can point at a real provider model name.
