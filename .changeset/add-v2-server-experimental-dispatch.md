---
"@mirri-ai/mirri-code": minor
---

Add experimental v2 backend dispatch for `mirri server run`. Set `MIRRICODE_EXPERIMENTAL_MULTI_SERVER=1` to start the kap-server (v2) backend instead of the v1 server.
