---
"@mirri-ai/agent-core-v2": minor
"@mirri-ai/klient": minor
"@mirri-ai/mirri-code-sdk": minor
"@mirri-ai/mirri-code": minor
---

Add an engine-side contributed-command registry exposed over the v2 `listCommands` / `runCommand` RPC and wired through the klient contract and the SDK. v2 SDK hosts can list and run contributed commands with `listCommands()` / `runCommand()`.