---
"@mirri-ai/mirri-code": minor
---

Parse `support_efforts` and `default_effort` from custom registry model entries. Models declaring effort levels now automatically get the `thinking` capability, even without `reasoning: true`.
