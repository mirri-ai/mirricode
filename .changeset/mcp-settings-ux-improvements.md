---
"@mirri-ai/mirri-code": patch
---

web: Improve MCP settings — show enabled/total tool count per server in the list, add search filter and scrollable max-height to the tool toggle modal, show tool descriptions as hover tooltips instead of inline text, and stop the edit form from touching tool toggle settings.
