---
"@mirri-ai/mirri-code": minor
---

web: Fix the Add Provider flow on the desktop — the model catalog loads, re-importing a provider only appends new models without overwriting existing settings or restoring deleted ones, and a manual form creates a provider with models.
