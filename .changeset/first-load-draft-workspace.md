---
"@mirri-ai/mirri-code": patch
---

web: Have the web UI open an empty new-task conversation on first launch, loading history into the sidebar in the background, so the pane is usable before the session list finishes loading.