---
"@mirri-ai/mirri-code": minor
---

Stop serving the web UI from `mirri server run` — the web UI is served by `mirri web` (the v2 engine) only, and the TUI `/web` command now launches that server.
