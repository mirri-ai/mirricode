---
"@mirri-ai/mirri-code": patch
---

Recognize kimi ecosystem plugin manifest names (`kimi.plugin.json`, `.kimi-plugin/plugin.json`) as a fallback when no Mirri manifest is present, so plugins that ship only a kimi manifest can be installed.
